# 📱 手机遥控 Claude Code · 详细使用教程

这份是给你看的「手把手」教程。技术细节在 [README.md](README.md)，日常用看这份就够。

---

## 一、它是干嘛的

电脑上跑一个小程序，手机打开一个网页，就能在手机上指挥电脑里的 Claude Code 干活：
下指令、看它写代码/跑命令、需要权限时在手机上点「允许 / 拒绝」。
这样你人不在电脑前、甚至带着手机出门，也能控制电脑继续干活。

---

## 二、最快上手（出门也能用，推荐）

> 你的 WiFi 被 Windows 标成了「公用网络」，普通局域网方式会被防火墙挡住。
> 所以**首选下面这个「隧道」方式**，它不挑网络、不用动防火墙。

### 第 1 步：开电脑端
双击文件夹里的 **`start.bat`**（`start-remote.bat` 现在也一样）。
会弹出一个黑色窗口，并在**十几秒后自动在电脑浏览器打开一个「控制台」网页**，它会自动：
- 启动服务器 + Cloudflare 隧道 + 防睡眠
- 在控制台里显示**大二维码**、公网网址、运行状态、实时日志

👉 **那个黑窗口不要关**！关了电脑端就停了。（控制台网页可以随便关，不影响）

### 第 2 步：手机扫码
华为手机用**「扫一扫」**（下拉控制中心里有，或相机/华为浏览器里也有），
扫控制台网页上的那个**大二维码**。
扫不了的话，就照着控制台里那行网址，在华为浏览器手动输入。

> 这次手机用流量、连别的 WiFi 都行，**不需要和电脑同一个网**。

### 第 3 步：开始用
手机上会出现聊天界面，在最下面输入框打字给 Claude 下指令，按发送即可。
电脑这边的控制台能看到「已连接 1 台手机」和实时日志。

### 控制台能干啥
- **↻ 重新生成地址**：换一个新的公网网址（旧的失效）。
- **模型 / 思考档位 / 全自动**：在电脑上直接调，和手机端同步。
- **工作目录**：点「浏览…」像资源管理器一样选文件夹，或直接填路径。
- **实时日志**：看 Claude 正在干啥、连了几台手机。

### 结束
回到电脑那个黑窗口按 **Ctrl + C**，或直接关掉窗口——服务器、隧道、防睡眠全部一起停，公网入口随之关闭（更安全）。
下次再双击 `start.bat` 即可（会自动顶替任何残留的旧进程）。

---

## 三、两种启动方式，区别在哪

| | `start-remote.bat`（隧道·推荐） | `start.bat`（局域网） |
|---|---|---|
| 手机和电脑要同一个 WiFi 吗 | **不用**，出门流量都行 | **要**，必须同一个 WiFi |
| 要不要动防火墙 | 不用 | 要（你的 WiFi 是「公用」，默认被挡） |
| 网址 | 公网 https，**每次重启会变** | 固定的 `http://10.38.7.142:8765/...`（在家不变） |
| 能装成手机 App | 能（https 才支持） | 受限 |
| 适合 | **出门 / 在家都行** | 仅在家、且愿意开防火墙 |

简单说：**先用 `start-remote.bat`**。`start.bat` 是备选。

---

## 四、你问的：「带 token 的完整网址」是啥

完整网址 = 网址主体 + 后面 `?token=...` 那一串。例子：

```
https://train-lows-ties-recognized.trycloudflare.com/?token=b54156ab1a95d2f033201e758bb09c05
└──────────── 网址主体 ────────────┘└──────── token（密码，必须带）────────┘
```

- **token 就是你这台电脑的「钥匙」**。手机带着它访问，才能控制电脑。
- 别人就算知道前面的网址主体，**没有 token 也连不上、控制不了**。
- 所以：**不要把带 token 的完整网址截图发给别人、发到群里**。给别人看网址=把钥匙给了别人。
- 你的 token 是固定的（存在 `config.json` 里）：`b54156ab1a95d2f033201e758bb09c05`
  （想换成自己记得住的，改 `config.json` 里的 `token` 即可，改完手机要重新扫码。）

二维码里其实已经把「网址主体 + token」都包含进去了，所以**扫码最省事**，不用手输那一长串。

---

## 五、你问的：「静态地址 / 同一个 WiFi」有关系吗

分三种「地址」给你讲清楚：

1. **局域网地址**（`http://10.38.7.142:8765/...`）
   - 只有手机和电脑**连同一个 WiFi**时能用，是这个 WiFi 内部的地址。
   - 走 `start.bat`。你这台电脑因为 WiFi 是「公用网络」，还得先放行防火墙，否则连不上。

2. **隧道公网地址**（`https://xxxx.trycloudflare.com/...`）
   - **任何网络都能用**，和「是不是同一个 WiFi」没关系。
   - 缺点：是临时的，**每次重启 `start-remote.bat` 都会换一个新网址**，所以要重新扫一次码。
   - 走 `start-remote.bat`。

3. **固定公网地址**（比如 `https://claude.你的域名.com`）—— 目前还没配
   - 永远不变、还能开机自启。需要一个**免费 Cloudflare 账号 + 一个域名**，配置一次即可。
   - 如果你嫌每次网址都变、要重新扫码麻烦，可以让我帮你升级成这种。

所以：**只要你用隧道方式（start-remote.bat），手机和电脑是不是同一个 WiFi 都无所谓**。
你说「手机和电脑用的同一个 [WiFi]」——那很好，在家两种方式都能试；但出门就只能靠隧道。

---

## 六、手机界面怎么用

- **底部输入框**：打字下指令，点右边圆形 ↑ 发送。
- **顶部小圆点**：绿色=已连接，橙色闪=正在干活，灰色=断开。
- **右上角「全自动」开关**：
  - **关**（默认）：Claude 要执行命令、改文件这类操作时，手机会弹窗让你点「允许 / 拒绝」。安全。
  - **开**：什么都不问，Claude 自动执行一切（相当于电脑上的 `--dangerously-skip-permissions`）。省事但有风险，确认环境可信再开。
- **权限弹窗**的四个选择：
  - 允许：放行这一次
  - 拒绝：不执行
  - 总是允许此工具：这次开机内同类操作不再问
  - 从现在起全自动：直接打开「全自动」
- **右上角 📁 文件夹按钮**（资源管理器）：点开能像电脑一样浏览磁盘和文件夹——
  - 点文件夹进入，点「⬆ 上一级」返回；
  - 「＋ 新建文件夹」在当前位置建一个新文件夹；
  - 「✓ 就在这里工作」= 把 Claude 的工作目录切到当前文件夹。
  - 想跑以前的老项目，就用它一路点进那个项目文件夹，再点「就在这里工作」即可。
- **右上角 ⚙ 设置**：
  - **模型**：Opus 4.8（最强）/ Sonnet 4.6（快·均衡）/ Haiku 4.5（最快最省）。切换**即时生效、保留当前对话**。
  - **思考档位 effort**：low / medium / high（默认）/ xhigh / max，越高思考越深入但越慢越贵。切换会**重开一个新对话**。
  - 也能手动输入「工作目录」路径（效果同上，适合你已知道完整路径时）。
  - 改服务器地址 / token（一般不用动；换电脑或换 token 时才用）。
- **左下角 ■ 停止**：Claude 干活时点它可以打断当前任务。

---

## 七、把它装成手机 App（可选）

用隧道方式（https）打开网页后：
- **华为浏览器**：右下角菜单 → 「添加到主屏幕」
- 之后桌面上就有个图标，点开像独立 App，不用每次开浏览器找网址。

---

## 八、常见问题

### ❓ 双击 .bat 没有二维码，只有一堆字 / 红色报错 `EADDRINUSE`
说明 **8765 端口已经被另一个本程序占着了**（多半你已经开着一个黑窗口，或上次没关干净）。
- 用 **`start-remote.bat`**：新版已经会**自动顶掉旧的**，直接双击它就行。
- 如果还不行：把所有黑窗口都关掉，等 3 秒，再双击 `start-remote.bat`。
- 实在不行：重启电脑，再双击。

### ❓ 启动时有个小黑窗一闪而过，但手机能正常连
那是「防睡眠」的后台小窗口闪了一下，不影响——服务器、隧道都在正常跑。新版已加 `windowsHide` 把它藏掉，重启一次就不闪了。

### ❓ 手机打开是空白 / 转圈 / 连不上
- 用隧道方式（start-remote.bat）：确认电脑那个黑窗口还开着、没报错；网址有没有输全（含 token）。
- 隧道网址**重启会变**，别用昨天那张旧二维码，用**这次新弹出的**。

### ❓ 提示「Token 错误」
手机用的 token 和电脑对不上。重新扫**这次**弹出的二维码即可（码里带最新 token）。

### ❓ 黑窗口里报 `claude` 相关错误 / 会话出错
多半是电脑上的 Claude Code 没登录。关掉，在电脑终端单独运行一次 `claude` 登录后，再启动本程序。

### ❓ 想换端口（8765 老被占）
打开 `config.json`，把 `"port": 8765` 改成别的（比如 `8766`），保存后重新启动。

### ❓ cloudflared 下载失败 / 提示找不到
文件夹里要有 `cloudflared.exe`。没有的话去
<https://github.com/cloudflare/cloudflared/releases/latest>
下载 `cloudflared-windows-amd64.exe`，改名为 `cloudflared.exe` 放进本文件夹。

---

## 九、安全须知 ⚠️

用隧道方式时，你的电脑就能从公网被访问到了，请务必：
1. **不要泄露带 token 的完整网址**（=钥匙）。别截图发人。
2. 不信任的网络、长时间无人看管时，**别开「全自动」**，用手机弹窗逐个确认。
3. 不用就**关掉黑窗口**，公网入口随之关闭。
4. 想换钥匙：改 `config.json` 的 `token`，手机重新扫码。

---

## 十、问答（你问过的，重要）

**Q：在家其实用不上手机控制，局域网那套（start.bat）还要吗？**
留着就行，不用管。在家直接用电脑最方便；`start-remote.bat` 出门在家都能用，所以平时只点它一个即可。

**Q：终端里打印的 `Token: xxxx` 那一串我要怎么用？扫码还需要管它吗？**
**扫二维码就完全不用管 token，也不用管网址前面的 xxxx**——二维码里已经把「完整网址 + token」打包好了，扫了直接进。
那行 `Token:` 只是给你「手动输网址」时备用的。手动时才把 `https://xxxx.trycloudflare.com/?token=你的Token`
里的 `xxxx` 换成实际地址、`你的Token` 换成你的 token。能扫码就别手输。

**Q：电脑到底下载 Cloudflare 了没有？想要固定网址，账号和域名免费吗？**
- 已经下载好了：文件夹里的 `cloudflared.exe`（约 52MB）就是，不用再下。
- Cloudflare **账号免费**。但「Cloudflare 固定网址」需要你**拥有一个域名**，而**域名通常要花钱**（.com 约 ¥70/年），不是免费的。
- 想要**完全免费的固定网址**，有两条不用买域名的路：
  - **ngrok**：免费版送 1 个固定网址（形如 `xxx.ngrok-free.app`），不变、自带 https。注册个免费账号即可。
  - **Tailscale**：免费，给你一个固定的内网地址，手机也装上就能连，更私密（不暴露到公网）。
  - 这两个我都能帮你配。（注意：在国内，ngrok/Cloudflare 有时不稳，Tailscale 相对看运气，最稳的是用一台国内服务器中转，但要花钱。）

**Q：扫码后提示「添加到桌面」，以后网址变了，这个桌面图标还有用吗？**
**临时隧道（trycloudflare）会变网址，所以那个桌面图标在下次重启后就失效了**（它记死了旧网址）。
要想桌面图标永久可用，必须先配一个**固定网址**（见上一条），用固定网址装的图标才一直好使。

**Q：电脑熄屏了还能用手机控制吗？**
- **只是屏幕黑了（显示器休眠）**：能用，程序照常跑。
- **电脑进入「睡眠」**：不能用，睡眠会把程序挂起。
- 好消息：`start-remote.bat` 运行期间我已加了**防睡眠**——只要那个黑窗口开着，电脑就不会睡，屏幕可以正常熄灭。关掉窗口自动恢复。
- 如果还是会睡，去 Windows 设置把「接通电源时——使设备进入睡眠状态」改成「从不」。

**Q：默认在哪个文件夹工作？能不能浏览/新建/选择文件夹？**
- 默认工作目录就是本程序所在的文件夹（`config.json` 里的 `cwd`）。
- **已经给你做好了**：手机右上角点 **📁**，就能像资源管理器一样浏览磁盘、进出文件夹、新建文件夹、并「就在这里工作」。
  跑以前的项目，点进那个项目文件夹按「就在这里工作」即可。

**Q：手机要开 VPN 吗，还是国内网络直接能用？**
- 你刚才扫码就连上了，说明**你当前的手机网络能直接访问，不用开 VPN**。
- 但 Cloudflare 隧道在国内有时会不稳定，万一哪天连不上，可以试开 VPN，或换成 Tailscale / 国内服务器中转的方案。

---

## 十一、文件夹里都是啥

| 文件 | 作用 |
|---|---|
| `start-remote.bat` | **出门可用·一键启动**（服务器+隧道+二维码）👈 平时双击这个 |
| `start.bat` | 局域网启动（同 WiFi，需开防火墙） |
| `server.js` | 电脑端服务器（程序主体） |
| `remote.js` | 隧道启动器（被 start-remote.bat 调用） |
| `public/` | 手机网页界面 |
| `config.json` | 配置：端口 / token / 工作目录 |
| `cloudflared.exe` | Cloudflare 隧道工具 |
| `qr-remote.png` | 最近一次的公网二维码（自动生成） |
| `qr.png` | 最近一次的局域网二维码（自动生成） |
| `README.md` | 技术说明 |
| `使用教程.md` | 本文件 |
